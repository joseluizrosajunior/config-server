
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        encrypt:
            enabled: false
        git:
          uri: https://github.com/joseluizcri/microservices-repo/
          search-paths: oauth, maintence